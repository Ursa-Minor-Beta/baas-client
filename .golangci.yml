run:
  skip-files: [ ]

linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - ineffassign
    - gofumpt
    - gosimple
    - unused
    - staticcheck
    - gci

linters-settings:
  gci:
    sections:
      - standard # Standard section: captures all standard packages.
      - default # Default section: contains all imports that could not be matched to another section type.
      - prefix(golang.org) # Custom section: groups all imports with the specified Prefix.
      - prefix(github.com) # Custom section: groups all imports with the specified Prefix.
      - prefix(github.com/simple-container-com/go-aws-lambda-sdk) # Custom section: groups all imports with the specified Prefix.
      - prefix(github.com/Ursa-Minor-Beta/baas-client) # Custom section: groups all imports with the specified Prefix.
    skip-generated: false
    custom-order: true
  revive:
    rules:
      - name: var-naming # usually this is not critical
        disabled: true
